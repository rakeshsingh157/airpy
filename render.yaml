services:
  - type: web
    name: tensorflow-api
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
    env: python
    plan: free
    environment:
      - key: PYTHON_VERSION
        value: 3.10.13
